import React from 'react';
import ReactDOM from 'react-dom/client';
import GameUI from './GreyStratumUI';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GameUI />
  </React.StrictMode>
);
